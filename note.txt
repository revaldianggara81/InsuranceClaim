#build
docker build -t claims_db-medallion .

# jika ada perubahan, stop container lama
docker stop claims_db-medallion
docker rm claims_db-medallion

# run
docker run -d \
-p 1521:1521 \
-p 5500:5500 \
-e ORACLE_PWD=Osc-indonesia123! \
--name claims_db-medallion \
claims_db-medallion

# cek
docker ps

# cek log oracle start
docker logs -f claims_db-medallion

# if oracle start sucesffully, then show like this
DATABASE IS READY TO USE!
Running init.sql
Running Bronze Layer
Running Silver Layer
Running Gold Layer

# === cek via container
docker exec -it claims_db-medallion bash


# login sysdba
sqlplus 'sys/Osc-indonesia123!@FREEPDB1 as sysdba'

# login bronze
sqlplus claims_bronze/claims_bronze@FREEPDB1

# login silver
sqlplus claims_silver/claims_silver@FREEPDB1

# login gold
sqlplus claims_gold/claims_gold@FREEPDB1

# check how many table
SELECT table_name FROM user_tables ORDER BY table_name;
